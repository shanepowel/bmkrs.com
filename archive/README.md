# Archive

Inactive code kept for reference. The live site is **`web/`** (Next.js 15 + Sanity).

| Path | What it was |
|------|-------------|
| `legacy-public-site/public_html/` | Legacy PHP + React 17 CDN site (image source for `web/scripts/sync-assets.js`) |
| `amplified/` | Separate project |
| `benative/` | Separate monorepo (Turbo) |
| `ftp-stub/` | Old hosting `.ftpquota` stub — not the Next.js app |
| `docs/` | Pre-Next deployment notes (`DEPLOYMENT.md`, `deploy.sh`, etc.) |

Do not set Vercel **Root Directory** to anything under `archive/`.
