# Not the Next.js app

This folder is a legacy hosting artifact (`.ftpquota` only). Do **not** use it as the Vercel **Root Directory**.

The site lives in **`/web`** at the repository root (`web/package.json`).
