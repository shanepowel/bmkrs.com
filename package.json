{
  "name": "bmkrs.com",
  "version": "2.0.0",
  "description": "BMKRS — design and growth studio (Next.js)",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix web",
    "install:web": "cd web && npm ci --legacy-peer-deps",
    "build": "npm run install:web && npm run build --prefix web",
    "vercel-build": "npm run build",
    "lint": "npm run lint --prefix web",
    "start": "npm run start --prefix web"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/shanepowel/bmkrs.com.git"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "next": "^16.2.7",
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  }
}
